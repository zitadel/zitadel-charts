name: Bump Version

on:
  repository_dispatch:
    types:
    - zitadel-released 

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
    - name: Skip Auto Bumping
      run: echo "Skipping auto bumping for chart version 8.12.0"
