Thank you for installing {{ .Chart.Name }}.

An admin service key has been configured for this installation.
To extract the private key for signing admin API JWTs:

  kubectl -n {{ .Release.Namespace }} get secret {{ include "zitadel.adminServiceKeySecretName" . }} -o jsonpath='{.data.tls\.key}' | base64 -d
