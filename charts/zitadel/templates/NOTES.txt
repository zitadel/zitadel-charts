Thank you for installing {{ .Chart.Name }}.

{{- if and (not (empty .Values.zitadel.configmapConfig.FirstInstance)) (and (not (empty .Values.zitadel.configmapConfig.FirstInstance.Org)) (not (empty .Values.zitadel.configmapConfig.FirstInstance.Org.Machine))) }}
If this is you first time deploying ZITADEL, you can now extract your service account key with:

  $ kubectl -n {{ .Release.Namespace }} get secret {{ .Values.zitadel.configmapConfig.FirstInstance.Org.Machine.Machine.Username }} -o jsonpath='{ .data.{{ .Values.zitadel.configmapConfig.FirstInstance.Org.Machine.Machine.Username }}\.json }' | base64 -D


{{- end}}