{{- if .Values.login.gateway.httpRoute.enabled -}}
{{- $fullName := include "zitadel.login.fullname" . -}}
{{- $svcPort := .Values.login.service.port -}}
{{- $externalDomain := .Values.zitadel.configmapConfig.ExternalDomain -}}
{{- $filters := .Values.login.gateway.httpRoute.filters -}}
{{- $timeouts := .Values.login.gateway.httpRoute.timeouts -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $fullName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "login.labels" . | nindent 4 }}
    {{- with .Values.login.gateway.httpRoute.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.login.gateway.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.login.gateway.httpRoute.parentRefs }}
  parentRefs:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if or .Values.login.gateway.httpRoute.hostnames $externalDomain }}
  hostnames:
    {{- if .Values.login.gateway.httpRoute.hostnames }}
    {{- range .Values.login.gateway.httpRoute.hostnames }}
    - {{ . | quote }}
    {{- end }}
    {{- else }}
    - {{ $externalDomain | quote }}
    {{- end }}
  {{- end }}
  {{- if .Values.login.gateway.httpRoute.paths }}
  rules:
    {{- range .Values.login.gateway.httpRoute.paths }}
    - matches:
        - path:
            type: {{ default "PathPrefix" .pathType }}
            value: {{ .path | quote }}
      backendRefs:
        - name: {{ $fullName }}
          port: {{ $svcPort }}
      {{- with $filters }}
      filters:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $timeouts }}
      timeouts:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- end }}
  {{- else }}
  {{- fail "login.gateway.httpRoute.paths must not be empty when login.gateway.httpRoute is enabled" }}
  {{- end }}
{{- end }}
