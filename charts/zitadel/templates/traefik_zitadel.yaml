{{- /* Traefik Ingress and Middlewares for the main ZITADEL service. */ -}}
{{- if .Values.ingress.traefik.enabled -}}
{{- $fullName := include "zitadel.fullname" . -}}
{{- $svcPort := .Values.service.port -}}
{{- $ns := .Release.Namespace -}}
{{- $mwPrefix := printf "%s-%s-traefik" $ns $fullName -}}
{{- $mwList := list -}}
{{- if .Values.ingress.traefik.middlewares.redirectHttps.enabled -}}
{{- $mwList = append $mwList (printf "%s-redirect-https@kubernetescrd" $mwPrefix) -}}
{{- end -}}
{{- if .Values.ingress.traefik.middlewares.cors.enabled -}}
{{- $mwList = append $mwList (printf "%s-cors@kubernetescrd" $mwPrefix) -}}
{{- end -}}
{{- if .Values.ingress.traefik.middlewares.inFlightReq.enabled -}}
{{- $mwList = append $mwList (printf "%s-inflight@kubernetescrd" $mwPrefix) -}}
{{- end -}}
{{- if .Values.ingress.traefik.middlewares.rateLimit.enabled -}}
{{- $mwList = append $mwList (printf "%s-ratelimit@kubernetescrd" $mwPrefix) -}}
{{- end -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-traefik
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zitadel.labels" . | nindent 4 }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: {{ .Values.ingress.traefik.entrypoints }}
    {{- if .Values.ingress.traefik.serversSchemeH2c }}
    traefik.ingress.kubernetes.io/service.serversscheme: h2c
    {{- end }}
    {{- if $mwList }}
    traefik.ingress.kubernetes.io/router.middlewares: {{ join ",\n      " $mwList }}
    {{- end }}
    {{- with .Values.ingress.traefik.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.traefik.className }}
  rules:
    {{- $values := .Values -}}
    {{- range .Values.ingress.traefik.hosts }}
    - host: {{ .host | default $values.zitadel.configmapConfig.ExternalDomain | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
    {{- end }}
  {{- with .Values.ingress.traefik.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- if .Values.ingress.traefik.middlewares.cors.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ $fullName }}-traefik-cors
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zitadel.labels" . | nindent 4 }}
spec:
  headers:
    accessControlAllowCredentials: {{ .Values.ingress.traefik.middlewares.cors.accessControlAllowCredentials }}
    {{- with .Values.ingress.traefik.middlewares.cors.accessControlAllowOriginListRegex }}
    accessControlAllowOriginListRegex:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    accessControlAllowMethods:
      {{- toYaml .Values.ingress.traefik.middlewares.cors.accessControlAllowMethods | nindent 6 }}
    accessControlMaxAge: {{ int64 .Values.ingress.traefik.middlewares.cors.accessControlMaxAge }}
    accessControlAllowHeaders:
      {{- toYaml .Values.ingress.traefik.middlewares.cors.accessControlAllowHeaders | nindent 6 }}
    addVaryHeader: {{ .Values.ingress.traefik.middlewares.cors.addVaryHeader }}
{{- end }}
{{- if .Values.ingress.traefik.middlewares.rateLimit.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ $fullName }}-traefik-ratelimit
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zitadel.labels" . | nindent 4 }}
spec:
  rateLimit:
    average: {{ .Values.ingress.traefik.middlewares.rateLimit.average }}
    burst: {{ .Values.ingress.traefik.middlewares.rateLimit.burst }}
    period: {{ .Values.ingress.traefik.middlewares.rateLimit.period }}
    sourceCriterion:
      {{- toYaml .Values.ingress.traefik.middlewares.rateLimit.sourceCriterion | nindent 6 }}
{{- end }}
{{- if .Values.ingress.traefik.middlewares.inFlightReq.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ $fullName }}-traefik-inflight
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zitadel.labels" . | nindent 4 }}
spec:
  inFlightReq:
    amount: {{ .Values.ingress.traefik.middlewares.inFlightReq.amount }}
    sourceCriterion:
      {{- toYaml .Values.ingress.traefik.middlewares.inFlightReq.sourceCriterion | nindent 6 }}
{{- end }}
{{- if .Values.ingress.traefik.middlewares.redirectHttps.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ $fullName }}-traefik-redirect-https
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zitadel.labels" . | nindent 4 }}
spec:
  redirectScheme:
    scheme: {{ .Values.ingress.traefik.middlewares.redirectHttps.scheme }}
    permanent: {{ .Values.ingress.traefik.middlewares.redirectHttps.permanent }}
{{- end }}
{{- end }}
